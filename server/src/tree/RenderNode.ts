import CSSOMNode from "./CSSOM/CSSOMNode";
import DOMNode from "./DOM/DOMNode";

class RenderNode {

    children: [RenderNode];
    cssomNode: CSSOMNode;
    domNode: DOMNode;

    constructor() {
        
    }

}

export default RenderNode;
